import { MatchfilterPipe } from './matchfilter.pipe';

describe('MatchfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
